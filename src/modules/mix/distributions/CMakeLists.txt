add_library(mixDistributions OBJECT DNormMix.cc DBetaBin.cc DNormMix.h DBetaBin.h)
target_include_directories(mixDistributions PRIVATE ${CMAKE_SOURCE_DIR}/src/Include)
if(NOT WIN32)
	target_compile_options(mixDistributions PRIVATE -fPIC)
endif()